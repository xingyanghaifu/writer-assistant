@echo off
REM Linux packaging script (v3 P4)
REM ASCII-only wrapper. Real build must run on Linux.

echo [INFO] Linux build must run on Linux.
echo        Use:  bash scripts/build.sh linux
echo        (this .cmd is a placeholder for cross-platform consistency)
exit /b 1