@echo off
REM macOS packaging script (v3 P4)
REM ASCII-only wrapper. Real build must run on macOS.

echo [INFO] macOS build must run on macOS.
echo        Use:  bash scripts/build.sh mac
echo        (this .cmd is a placeholder for cross-platform consistency)
exit /b 1